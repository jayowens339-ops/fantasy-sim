{
  "name": "fantasy-sim",
  "version": "1.0.0",
  "description": "Full-stack Fantasy Sports Simulator (frontend + backend)",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
